# Publish replaces the Public Content Snapshot; do not schedule publication

The public site reads the Public Content Snapshot for title, summary, slug, content, publication date, and pin state. Category and Tag assignments use the current Post relations and do not require republishing. This personal blog favors one set of assignments and ordinary relation counts over separate public associations; caches may lag, and search labels refresh on publish or index rebuild. Reading time is derived from content when rendering and is not stored. Code blocks are highlighted on the server when publishing and stored as HTML in the snapshot. Unchanged code blocks keep highlighting already in the snapshot. Visitors do not highlight on the client. Publishing replaces that snapshot from the Post the Admin is editing; autosave does not. Publishing does not generate a summary. Unpublishing discards the snapshot. Draft versus published is the outcome of those two actions, not a separate status the Admin toggles and later syncs. A Published Post is public immediately, so there is no scheduled publication. The publication date is for display and listing order, taken from server time on first publish if missing, and it cannot be in the future. A Post Revision is created when publishing, and again immediately before restoring another revision so unpublished edits are not lost. Autosave does not create a Post Revision. Restoring a Post Revision updates only the Post the Admin is editing. Changing the slug and publishing again leaves the old public slug unreachable. Publishing is idempotent: the same request updates the snapshot, Search Index, and Public Cache, and the Admin can run it again if it fails. There is no auto-snapshot queue and no post-process workflow.
