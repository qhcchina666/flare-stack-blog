# Deleted Comments stay as placeholders

Deleting a Comment marks it deleted instead of removing the row, so a Comment Thread does not break and Reply targets remain. Public counts skip deleted Comments. A thread whose root is deleted and that has no published replies is omitted. A deleted root with published replies, and any deleted reply in a visible thread, still render as placeholders.
